#this is intended to calculate the perimeter and area of a circle

radius = float(input("Please input radius value: "))

diameter = radius * 2

area = radius ** 2 * 3.14

perimeter = radius * 2 * 3.14

print ("The diameter of such circle would be {}" .format(diameter))

print ("The area of such circle would be {}" .format(area))

print ("The perimeter of such circle would be {}" .format(perimeter))
